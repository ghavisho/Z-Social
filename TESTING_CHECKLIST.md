# چک‌لیست تست دستی Z

مهم: این یک **چک‌لیست**، نه گزارش نتیجه‌ی تست است. من (Claude) نتوانستم هیچ‌کدام از این‌ها را واقعاً اجرا کنم — نه npm install، نه اتصال به Supabase واقعی، چون در این محیط توسعه به اینترنت دسترسی نبود. کد بر اساس مرور دقیق، مطابقت Schema/RLS/props، و منطق نوشته شده، اما تنها راه اطمینان قطعی این است که خودت هرکدام از موارد زیر را روی نسخه‌ی Deploy‌شده امتحان کنی.

هر مورد رو که تست کردی، جلوش ✅ یا ❌ بذار. اگه ❌ بود، دقیقاً چی شد رو برام بفرست.

## احراز هویت
- [ ] ثبت‌نام با نام‌کاربری و رمز جدید
- [ ] ثبت‌نام با نام‌کاربری تکراری → باید خطا بدهد
- [ ] ورود با نام‌کاربری/رمز درست
- [ ] ورود با رمز اشتباه → باید خطا بدهد
- [ ] خروج از حساب (Settings → خروج)
- [ ] تغییر رمز عبور
- [ ] `npm run seed:admin` → ساخت Super Admin، ورود، اجبار به تغییر رمز

## دوستی / بلاک / گزارش
- [ ] ارسال درخواست دوستی از Discover یا پروفایل
- [ ] پذیرفتن درخواست از صفحه‌ی `/profile/requests`
- [ ] رد کردن درخواست
- [ ] لغو درخواست ارسالی
- [ ] حذف دوست
- [ ] مسدود کردن کاربر
- [ ] رفع مسدودی
- [ ] گزارش کاربر/پست/کامنت

## Pulse (پست‌ها)
- [ ] ساخت پست متنی
- [ ] ساخت پست با عکس
- [ ] ساخت پست با ویدیو
- [ ] لایک کردن / برداشتن لایک
- [ ] ثبت کامنت
- [ ] حذف کامنت خودت
- [ ] حذف پست خودت
- [ ] پست دیگران درست نمایش داده می‌شود (عکس/ویدیو باز می‌شود، نه ۴۰۳)

## Moments
- [ ] ساخت Moment متنی
- [ ] ساخت Moment با عکس/ویدیو
- [ ] بعد از ۲۴ ساعت خودش پاک می‌شود (نیاز به صبر یا دستکاری دستی `expires_at` برای تست سریع‌تر)
- [ ] دیدن لیست بازدیدکننده‌ها (فقط وقتی صاحب Moment هستی)

## چت
- [ ] شروع مکالمه‌ی جدید از پروفایل یک دوست (دکمه‌ی «پیام»)
- [ ] فرستادن پیام متنی، دریافت آنی توسط طرف مقابل (Realtime)
- [ ] فرستادن پیام صوتی، پخش توسط طرف مقابل
- [ ] لیست پیام‌ها آخرین پیام و تعداد نخوانده را درست نشان می‌دهد
- [ ] باز کردن مکالمه، بج نخوانده پاک می‌شود

## تماس صوتی
- [ ] برقراری تماس بین دو کاربر (دو مرورگر/دستگاه جدا)
- [ ] قطع تماس
- [ ] میوت/آنمیوت میکروفون
- [ ] تست روی شبکه‌ی محدود (VPN شرکتی و مشابه) — احتمال نیاز به TURN

## اعلان‌ها
- [ ] لایک/کامنت/درخواست دوستی/بازدید Moment، بج زنده روی آیکون پالس ظاهر می‌شود
- [ ] صفحه‌ی اعلان‌ها همه را نشان می‌دهد و به‌عنوان خوانده‌شده علامت می‌زند

## ادمین
- [ ] ورود با حساب ادمین، دسترسی به `/admin/dashboard`
- [ ] کاربر عادی نمی‌تواند به `/admin/dashboard` برود (باید ریدایرکت شود)
- [ ] مشاهده‌ی لیست کاربران
- [ ] حذف یک پست از پنل ادمین
- [ ] حذف یک Moment از پنل ادمین
- [ ] باز کردن یک مکالمه از `/admin/messages` — یک ردیف جدید در `/admin/logs` با action = `admin_viewed_conversation` ظاهر شود
- [ ] `/admin/files` تعداد کاربران هر بادکت را نشان می‌دهد (بدون خطا)
- [ ] `/admin/settings` تعداد کاربران/ادمین‌ها را درست نشان می‌دهد
- [ ] حل‌کردن یک گزارش
- [ ] هرکدام از موارد بالا در `/admin/logs` ثبت شده باشد

## PWA
- [ ] نصب روی اندروید (Chrome)
- [ ] نصب روی آیفون (Safari → Add to Home Screen)
- [ ] نصب روی دسکتاپ (Chrome/Edge)
- [ ] باز کردن اپ نصب‌شده — بدون نوار آدرس مرورگر اجرا می‌شود

## تنظیمات
- [ ] آپلود عکس پروفایل
- [ ] تغییر تنظیمات حریم خصوصی و ذخیره‌شدن واقعی (رفرش صفحه و چک کردن مقدار)

---

اگه یک مورد رو تست کردی و کار نکرد، این اطلاعات رو برام بفرست تا سریع‌تر پیدا کنم مشکل کجاست:
1. کدوم مورد از چک‌لیست
2. پیام خطای دقیق (متن یا اسکرین‌شات)
3. مرورگر/دستگاهی که باهاش تست کردی
