# مستندات امنیتی Z

این سند خلاصه‌ای از تصمیمات امنیتی پروژه است — چه چیزی پیاده شده، چرا، و کجاها هنوز باید احتیاط کرد.

## احراز هویت (Authentication)

- ورود فقط با **نام‌کاربری + رمز عبور** است (بدون ایمیل واقعی). داخلی، هر یوزرنیم به یک ایمیل ساختگی (`username@z.local`) روی Supabase Auth نگاشت می‌شود — رمزها با همان مکانیزم امن خودِ Supabase Auth (bcrypt) هش می‌شوند، Z هرگز رمز خام را ذخیره نمی‌کند.
- Session از طریق کوکی امن مدیریت می‌شود (`@supabase/ssr`)، نه localStorage — کمتر در معرض حملات XSS است.
- Super Admin اولیه با یک اسکریپت جدا (`scripts/seed-admin.mjs`) ساخته می‌شود، نه از طریق فرم ثبت‌نام عمومی. بعد از اولین ورود، تغییر رمز اجباری است و رمز اولیه دیگر معتبر نیست (بدون Backdoor).

## کنترل دسترسی (Authorization / RLS)

- تقریباً تمام جدول‌ها **Row Level Security** فعال دارند — یعنی حتی اگر کلید anon لو برود، هرکس فقط به داده‌ای دسترسی دارد که Policy صریحاً اجازه داده (پیام‌های مکالماتی که عضوشان است، پست‌های دوستان/عمومی، و غیره).
- عملیات حساس (حذف توسط ادمین، خواندن گزارش‌ها، دسترسی مدیریتی) از طریق `service_role key` روی سرور انجام می‌شود — این کلید هرگز به کلاینت ارسال نمی‌شود (`lib/supabase/server.ts` این جداسازی را اعمال می‌کند).
- هر عملیات ادمین در جدول `admin_logs` ثبت می‌شود — این جدول هیچ Policy برای کلاینت ندارد؛ فقط با service_role قابل خواندن/نوشتن است.
- دسترسی مدیریتی به محتوای خصوصی مکالمات (`/admin/messages`) پیاده‌سازی و کاملاً Audit شده: پیش از نمایش هر پیامی، یک رکورد در `admin_logs` با action = `admin_viewed_conversation` ثبت می‌شود — حتی اگر ادمین هیچ پیامی را نخواند.

## Storage (فایل‌ها)

- تمام بادکت‌ها به‌جز `avatars` **خصوصی** هستند. دسترسی به فایل از طریق Signed URL کوتاه‌مدت (۱۰ دقیقه) انجام می‌شود، نه URL عمومی دائمی.
- Policyهای Storage بر اساس رابطه‌ی واقعی (عضویت در مکالمه، دوستی، مالکیت) تعریف شده‌اند — نه فقط «هرکس فایل خودش را ببیند» (این یک باگ اولیه بود که در طول توسعه پیدا و رفع شد؛ نگاه کنید به `supabase/migrations/0004_storage_read_fix.sql`).
- نوع فایل مجاز برای هر بادکت محدود شده (فقط تصویر/ویدیو/صوت/PDF) — فایل‌های اجرایی رد می‌شوند.

## ورودی‌ها و اعتبارسنجی

- تمام API Routeها ورودی را با `zod` اعتبارسنجی می‌کنند؛ ورودی نامعتبر رد می‌شود قبل از رسیدن به دیتابیس.
- فیلتر اسپم پایه (`lib/ai/moderation.ts`) همیشه فعال است، حتی بدون کلید AI.

## Rate Limiting

- ثبت‌نام، ورود، پست، کامنت، و درخواست دوستی محدود به نرخ مشخصی در دقیقه هستند (`lib/utils/rateLimit.ts`).
- **محدودیت شناخته‌شده:** این Rate Limiter در حافظه‌ی هر instance است، نه یک Store مشترک (مثل Redis) — روی چند Instance موازی، محدودیت واقعی «به‌ازای هر Instance» است نه سراسری. برای مقیاس بزرگ‌تر، `@upstash/ratelimit` جایگزین مناسبی است.
- **پیام‌های چت** از این Rate Limiter عبور نمی‌کنند چون مستقیماً از کلاینت به Supabase می‌روند (برای تأخیر کمتر در Realtime) — اما محدود نیستند: یک Trigger سطح دیتابیس (`supabase/migrations/0005_message_rate_limit.sql`) مستقل از مسیر ارسال، حداکثر ۳۰ پیام در دقیقه به‌ازای هر فرستنده را اعمال می‌کند.

## حالت آفلاین کامل شبکه (Maintenance Mode)

- فقط **super_admin** (نه admin معمولی) می‌تواند کل شبکه را قفل کند — این عمداً محدودتر از بقیه‌ی عملیات ادمینه، چون قدرت خیلی بالایی داره (حتی خودِ ادمین‌های معمولی رو هم قفل می‌کنه).
- بررسی وضعیت آفلاین در `middleware.ts` روی **هر درخواستی** (قبل از هر منطق دیگری) انجام می‌شود — یعنی حتی API Routeها هم مشمولش می‌شوند، نه فقط صفحات.
- جدول `app_settings` (تک‌ردیفه) هیچ Policy نوشتنی برای کلاینت ندارد — فقط از طریق `/api/admin/maintenance` با کلید service_role و بعد از چک نقش super_admin قابل تغییر است.
- روشن/خاموش‌شدنش در `admin_logs` ثبت می‌شود.

## سیستم بلاک دائمی (حساب / IP / دستگاه)

- هنگام ثبت‌نام و ورود، IP و یک شناسه‌ی تصادفی دستگاه (کوکی httpOnly، مستقل از حساب کاربری) ثبت می‌شود.
- ادمین می‌تواند از صفحه‌ی جزئیات هر کاربر (`/admin/users/[id]`) حساب را غیرفعال کند، یا IP/دستگاه را برای همیشه بلاک کند — بلاک شدن در `middleware.ts` روی **هر درخواستی**، حتی قبل از رسیدن به صفحه‌ی ورود، چک می‌شود.
- **صادقانه بگم:** بلاک IP و دستگاه قطعیت ۱۰۰٪ ندارند — IP عوض می‌شود، Cache/کوکی پاک می‌شود، دستگاه دیگری استفاده می‌شود. این‌ها سطح دسترسی رو بالا می‌برن، نه یک قفل فیزیکی. این محدودیت هر سیستم مشابهی (نه فقط Z) است.
- ادمین‌های معمولی نمی‌توانند ادمین دیگری (یا super_admin) را از طریق این سیستم بلاک کنند — فقط کاربران عادی.
- هر بلاک/غیرفعال‌سازی در `admin_logs` ثبت می‌شود.

## محتوای حذف‌شده — فقط برای مدیر اصلی، با برچسب قرمز

- پست، کامنت، و پیامِ حذف‌شده (`deleted_at` غیر null) در **سطح RLS** (نه فقط فیلتر سمت کلاینت) برای همه پنهان است — یعنی حتی اگر کسی کلید anon را داشته باشد و مستقیم کوئری بزند، چیزی نمی‌بیند. این تضمین در `supabase/migrations/0002_rls_policies.sql` (Policyهای select پست/کامنت/پیام) وجود دارد.
- فقط **super_admin** در پنل ادمین (`/admin/posts`، `/admin/messages/[id]`) محتوای حذف‌شده را می‌بیند — با یک برچسب قرمز واضح (`DeletedTag`) و متن خط‌خورده. ادمین معمولی همان چیزی را می‌بیند که یک کاربر عادی می‌دید (چیزی که حذف نشده).
- عکس/ویدیوی حذف‌شده هم شامل همین قانون است: چون بادکت‌های Storage خصوصی‌اند و Policyشان به وضعیت `deleted_at` پست/پیام گره خورده، بعد از حذف حتی از طریق مسیر عادی (`/api/media/...`) هم دیگر برای هیچ‌کس (نه‌حتی ادمین) قابل مشاهده نیست. برای همین یک مسیر جدا و محدود ساخته شد: `/api/admin/media/[bucket]/[...path]` که فقط بعد از چک صریح نقش admin/super_admin در دیتابیس (نه از طریق Storage RLS)، با کلید service_role یک Signed URL موقت می‌سازد. این مسیر هرگز نباید از جایی خارج از صفحات `/admin` لینک شود.

## زبان، تم، و رنگ — کوکی‌های ترجیحی (بدون داده‌ی حساس)

- زبان (`z_lang`)، حالت روشن/تاریک (`z_theme`)، و رنگ اصلی (`z_accent`) در کوکی‌های ساده (نه httpOnly، نه رمزنگاری‌شده) ذخیره می‌شوند — چون هیچ داده‌ی حساسی نیستند، فقط ترجیحات ظاهری. این کوکی‌ها به هیچ داده‌ی کاربری متصل نیستند و کسی نمی‌تواند با تغییرشان به چیزی دسترسی پیدا کند؛ بدترین اتفاق ممکن، دیدن اپ با زبان/رنگ اشتباه است.

## به‌روزرسانی از پنل ادمین (آپلود ZIP → Push به GitHub → Deploy خودکار)

این یکی از قدرتمندترین قابلیت‌های پنل ادمین است — قدرتش را باید صریح مستند کرد:

- **این قابلیت معادل اجرای کد دلخواه روی سایتت است.** هر کسی که به این صفحه دسترسی پیدا کند، عملاً می‌تواند هر چیزی را روی دامنه‌ی تو منتشر کند. برای همین **فقط super_admin** (نه admin معمولی) به آن دسترسی دارد — حتی سخت‌گیرانه‌تر از حالت آفلاین‌سازی شبکه.
- کد جدید هرگز به دیتابیس Supabase دسترسی ندارد یا آن را تغییر نمی‌دهد — چون Deploy کردن کد (روی Netlify/Vercel/هرچی) و نگه‌داشتن داده (روی Supabase) دو عملیات کاملاً مجزا روی دو سرویس متفاوت‌اند؛ این جداسازی معماری، نه یک قابلیت اضافه، همان چیزی است که تضمین می‌کند آپلود کد جدید هرگز داده‌ی کاربر را پاک نمی‌کند.
- **معماری این قابلیت عمداً میزبانی‌مستقل است:** سرور Z مستقیماً با Netlify یا Vercel صحبت نمی‌کند — فقط با GitHub API یک Commit جدید می‌سازد. هر سرویسی که به آن مخزن وصل باشد (Netlify، Vercel، Cloudflare Pages) خودش Build را شروع می‌کند. این یعنی اگر روزی هاست را عوض کنی، این قابلیت بدون هیچ تغییری در کد Z کار می‌کند.
- `GITHUB_TOKEN` باید یک **Fine-grained Personal Access Token** با دسترسی محدود به **فقط همین یک مخزن** و فقط **Contents: Read & Write** باشد — نه یک Classic Token با دسترسی کامل به همه‌ی مخازن حساب گیت‌هابت. اگر این کلید لو برود، بدترین اتفاق این است که کسی بتواند در همین یک مخزن Commit بزند، نه کل حساب گیت‌هابت.
- کلیدها فقط Environment Variable سمت سرورند — هرگز به کلاینت ارسال نمی‌شوند و در کد قابل مشاهده نیستند.
- ورودی (فایل zip) قبل از استفاده اعتبارسنجی می‌شود: محدودیت حجم (۵۰ مگابایت)، محدودیت تعداد فایل، و رد صریح مسیرهای شامل `..` (برای جلوگیری از Path Traversal).
- هر بار استفاده از این قابلیت در `admin_logs` با شناسه‌ی ادمین، تعداد فایل، و اسم فایل zip ثبت می‌شود.
- **این قابلیت تغییرات دیتابیس (migration) را خودکار اعمال نمی‌کند** — عمداً، چون اجرای خودکار SQL ناشناس روی دیتابیس تولید بسیار خطرناک‌تر از Deploy کد است. فایل‌های migration جدید همچنان باید دستی، توسط خودِ ادمین، در SQL Editor اجرا شوند.
- **هر بار کل مخزن جایگزین می‌شود** (Tree کاملاً جدید، بدون Merge با نسخه‌ی قبلی) — یعنی اگر یک بار فقط چند فایل ناقص آپلود شود، بقیه‌ی فایل‌های مخزن (از جمله کدهایی که کار می‌کردند) حذف می‌شوند. این رفتار عمدی و مطابق درخواست اصلی («هر دفعه کل فایل رو بدی») است، اما یعنی این قابلیت اشتباه‌بخشنده نیست — همیشه باید کل پروژه آپلود شود، نه یک زیرمجموعه.

## نکاتی که هنوز باز هستند (شفاف باشیم)

- بدون تست نفوذ واقعی (Penetration Test) — این سند فقط تصمیمات طراحی را مستند می‌کند، نه نتیجه‌ی یک ارزیابی امنیتی مستقل.
- TURN اختصاصی برای تماس صوتی تنظیم نشده — یک TURN رایگان و عمومی (openrelay.metered.ca) به‌عنوان fallback در کد هست، پس تماس روی اکثر شبکه‌ها کار می‌کند، اما این TURN بین همه‌ی اینترنت مشترک و نرخ‌محدود است. برای مقیاس واقعی، یک TURN اختصاصی (پولی) توصیه می‌شود.
- **کارایی Middleware:** با اضافه‌شدن چک آفلاین‌بودن و بلاک IP/دستگاه، هر درخواست (نه فقط بار اول) حالا ۲ تا ۳ کوئری اضافه به دیتابیس می‌زند، قبل از اینکه حتی به صفحه برسد. برای مقیاس فعلی (شبکه‌ی کوچک/متوسط) مشکلی ایجاد نمی‌کند، ولی اگر ترافیک خیلی بالا رفت، جایگزین بهتر این است که وضعیت آفلاین/لیست بلاک را در یک لایه‌ی سریع‌تر (مثل Vercel Edge Config یا یک Cache کوتاه‌مدت) نگه داریم، نه یک کوئری مستقیم به Postgres در هر درخواست.
- **کلید service_role در Middleware:** برای چک‌کردن بلاک IP/دستگاه، middleware مستقیماً از کلید service_role استفاده می‌کند (نه از Policy عمومی RLS) — این یک تصمیم آگاهانه است (چون این دو جدول هیچ Policy عمومی ندارند)، ولی یعنی این کلید قدرتمند در مسیر اجرای هر درخواست حاضر است. اگر باگی در خودِ کد Middleware پیدا شود، سطح دسترسی بالاتری نسبت به یک مسیر API معمولی دارد — چیزی که در بازبینی‌های امنیتی بعدی باید موردتوجه باشد.
- بلاک IP/دستگاه (توضیح داده‌شده در بالا) قطعیت فنی ۱۰۰٪ ندارد — این یک محدودیت شناخته‌شده‌ی نوع خودِ این مکانیزم است، نه یک باگ قابل‌رفع.

اگر آسیب‌پذیری امنیتی پیدا کردید، لطفاً قبل از افشای عمومی، مستقیماً به تیم توسعه گزارش دهید.
